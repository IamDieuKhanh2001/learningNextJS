/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "example.domain.com",
        ]
    }
}

module.exports = nextConfig
